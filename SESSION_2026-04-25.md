# SESSION 2026-04-25 — iPhone PUTTS bug fixed; layout refactored to elastic-stage

> Closes the iPhone-only rendering bug from earlier in the day and refactors the
> layout architecture to be robust against viewport variation.

---

## TL;DR

PUTTS row was missing on iPhone Safari because the `.screen` had `overflow: hidden`
and the footer's intrinsic height (with a 230px padding-bottom hack) exceeded the
remaining viewport space — so PUTTS was rendered into clipped territory below the
visible area. Fixed across v9.12 → v9.15. Layout is now: masthead fixed, white
stage **elastic**, footer **content-sized** with min-height matching across all
five screens for visual parity. Title font on non-hole screens now matches the
hole number size (56/46px). "Final Round Score" renamed to "Final Score" so it
fits at 46px on small screens.

---

## What shipped this session (v9.12 → v9.15)

### v9.12 — initial footer rebalance
- Dropped 230px padding-bottom hack on `.footer`
- Changed `.footer` `justify-content: flex-end` -> `flex-start`
- Lowered footer min-height to fit 6 rows snugly
- Trimmed small-screen masthead height 170 -> 150 (reclaimed ~30px)
- Trimmed small-screen score-section flex-basis 228 -> 210
- SW cache bumped v12 -> v13

### v9.12.1 — svh attempt
- Tried switching `.screen` from `100dvh` -> `100svh` to dodge iOS Safari URL bar
  overlay. Reverted in v9.13 (Paul flagged svh had caused scrollbar issues
  previously when content overflowed).

### v9.13 — architectural refactor
- **Stage elastic** (`flex: 1 1 auto`) — absorbs all viewport variation
- **Footer content-sized** (`flex: 0 0 auto`)
- Reverted to `100dvh` for `.screen` and body
- Robust against iOS Safari URL bar quirks, dvh ambiguity, device variation

### v9.13.1 — visual parity restored
- Added back `min-height: calc(220px + env(safe-area-inset-bottom))` on `.footer`
  so non-hole screens (Setup, midround, card, success) match the hole-screen
  footer height. No layout shift between screens.

### v9.14 — title size bump (round 1)
- `.header-lower-title` 26px -> 36px (default), 30px (small-screen)
- Helped but tops still didn't align with hole-num

### v9.15 — title size matches hole-num
- "Final Round Score" -> "Final Score" (so it fits at 46px on iPhone)
- `.header-lower-title` 56px default / 46px small-screen — exact match
- Tops and bottoms of titles now align with hole number's visual mass

### Service worker
- Bumped to v18 over the course of these patches
- Network-first for HTML / cache-first for assets — every deploy reaches devices
  on next page load without manual cache clears

---

## Locked architecture (do not revisit)

### Layout (post v9.13)
- `.screen { display: flex; flex-direction: column; height: 100dvh; overflow: hidden; }`
- Masthead: fixed, `flex-shrink: 0` (small-screen 150px + safe-area-top)
- Stage: elastic, `flex: 1 1 auto`, `overflow-y: auto` (white card)
- Footer: content-sized, `flex: 0 0 auto`, `min-height: calc(220px + safe-area-bottom)`,
  `padding: 20px 20px calc(env(safe-area-inset-bottom) + 12px)`
- All screens have identical masthead/stage/footer dimensions

### Title alignment
- `.hole-num`: 56px default / 46px small-screen
- `.header-lower-title`: matches exactly — 56px / 46px, letter-spacing -2px,
  line-height 1, white-space nowrap

### SW strategy
- Network-first for HTML and navigations
- Cache-first for assets (mp3, fonts, manifest)
- New deploys land on next page load. No more cache battles.

---

## Diagnostic learnings (for future Claude / Paul)

- iOS Safari's `100dvh` doesn't reliably exclude the bottom URL bar overlay in
  every state — using elastic stage architecture sidesteps the entire question.
- `?v=N` cache-busts work for HTTP cache but not for SW cache hits; the only
  reliable cure for sticky SWs is to fix the SW strategy (network-first for HTML).
- "Headroom didn't change between padding bumps" was the smoking gun — meant the
  footer was being clipped by `overflow: hidden`, not just under-padded.
- Paul's iPhone is 15 Pro Max — viewport ~700-750px in Safari. Small-screen rule
  (`max-height: 750px`) applies.
- Cross-device responsive design (`env(safe-area-*)`, `100dvh`, elastic flex)
  works equally well on Android Chrome / Samsung Internet.

---

## Touch-target observations (carry forward)

- `.putts-btn` is 26×26 px — under Apple's 44×44 HIG minimum. Easy mis-tap.
- `.switch` (FIR/GIR/etc. toggles) is 53×26 — borderline.
- Jewel score (92px), nav buttons (~50px effective) are fine.
- Suggested follow-up: bump `.putts-btn` to ~36-44px without making it visually
  overpower the row.

---

## Connector / tooling state (for reference)

- **Gmail** — connected; can read threads
- **Google Drive** — connected; can list/read files
- **Workspace folder** — `~/Documents/Studio/Golf` is fully read/write via Cowork
- **GitHub** — no MCP yet; Paul still pushes via GitHub Desktop. Future: dedicated
  GitHub connector for direct commits.
- **Live URL fetch** — sandbox is firewalled from `kamloopspaul-a11y.github.io`
  and `raw.githubusercontent.com`; Claude can't fetch the deployed page directly.
  Workarounds: ask Paul for screenshots, use Chrome MCP if extension is connected.
- **Chrome MCP** — extension live (Browser 1, macOS, local). Available for
  DOM probes / network inspection on demand.
- **Engineering plugin skills** — debug, code-review, system-design, architecture,
  tech-debt, testing-strategy, documentation, deploy-checklist, incident-response,
  standup. Used `engineering:debug` today.

---

## Lock-file mystery (resolved this session)

The previous lockup pattern (sandbox can't remove `.git/*.lock` due to macOS
extended ACLs) didn't recur during today's edits. The push from GitHub Desktop
went through cleanly. Possibly the .js file (sw.js) being edited via cat-heredoc
rather than Edit/Write avoided the issue. If it returns:
```
rm -f ~/Documents/Studio/Golf/.git/*.lock
rm -f ~/Documents/Studio/Golf/.git/objects/*.lock
rm -f ~/Documents/Studio/Golf/.git/refs/heads/*.lock
```

---

## Next session — pick ONE

1. **Touch-target bump** for `.putts-btn` (26 -> ~40) and review `.switch`. CSS-only.
2. **Stats summary on Posted! screen** — front/back/total, hit counts, avg Score.
3. **Tee selector** — Settings-tab one-time pick (Mt. Paul Blue + Red).
4. **Import 2024 historical CSV** — 62 rounds at `~/Documents/Studio/2024-migration.csv`.
5. **Set up GitHub connector** so Claude can commit + push without GitHub Desktop.

---

## Reference

- Live URL: https://kamloopspaul-a11y.github.io/golf-scores
- Repo: https://github.com/kamloopspaul-a11y/golf-scores
- Local: `~/Documents/Studio/Golf`
- Webhook `/exec` URL embedded in `index.html`
- Cache-bust pattern: append `?v=N`, bump N each test
- Today's last pushed version: v9.15 / SW v18
